import React from "react";
import '../assets/styles/github.css';

const GithubScreen = ({ githubRef }) => {
    return (
        <div className="githubScreen" ref={githubRef}>
        </div>
    );
}

export default GithubScreen;
