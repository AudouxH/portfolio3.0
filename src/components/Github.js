import React from "react";
import '../constants/styles/github.css';

const Github = ({ githubRef }) => {

    return (
        <div className="github" ref={githubRef}>
            
        </div>
    );
}

export default Github;